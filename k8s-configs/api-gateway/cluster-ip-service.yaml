apiVersion: v1
kind: Service
metadata:
  name: api-gateway-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: api-gateway
    tier: datascience
  ports:
    - port: 80
      targetPort: 3000